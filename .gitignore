# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase credentials — never commit
.env
.env.local

# Supabase CLI local state (project ref, pooler URL, version cache) —
# created by `supabase link`/`functions deploy`, machine-local, never
# committed (matches the CLI's own standard convention)
supabase/.temp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
